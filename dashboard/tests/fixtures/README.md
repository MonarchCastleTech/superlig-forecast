# Withdrawn dashboard fixture

`withdrawn-dashboard.json` is the 23 September 2026 candidate used by the dashboard component checks. It omitted completed official fixtures and matched only 15 of 18 current clubs to squad values. Its probabilities are invalid for publication and were removed from `dashboard/public/data/dashboard.json`. The fixture is retained to check rendering of a full payload and must not be copied into a public directory.
